# 水草缸 · Planted Tank

一個安靜的、可以放著不管的 three.js 水草缸。單一 HTML 檔，打開就能看。

> A calm, self-contained planted aquarium built with three.js. Single HTML file, no build step, no network requests at runtime.

**v1.0 · Doe × Claude**

---

## 這是什麼

一個 Amano 風格的 60 cm 水草缸：石組偏左、沉木偏右、後景水蘭貼著後壁、前景鋪莫絲，中央留出開闊的泳道。缸裡有金魚、神仙魚、海馬、河豚在游，底下有藍魔蝦、螃蟹和海星在爬。

沒有分數、沒有進度條、沒有通知。你可以餵食、敲缸壁、開夜燈，或者什麼都不做。

### 互動

| 動作 | 結果 |
|---|---|
| **餵食**（或按 `F`） | 飼料從水面撒下慢慢沉，魚偵測最近的一顆游過去吃。沒吃到的沉到底，被螯蝦和螃蟹撿走 |
| **點缸壁** | 漣漪從點擊處擴散，魚群壓低身體四散逃開，河豚鼓成球，螃蟹舉起前身衝向石頭躲藏。連續敲會習慣，反應變小 |
| **夜燈**（或按 `N`） | 轉成藍調微光，焦散變弱，魚速降到四成並沉底。螢光魚開始發光 |
| **拖曳／滾輪／雙指** | 環繞、縮放 |
| **游標或手指停在玻璃上不動** | 停留 0.9 秒後出現光暈，好奇心高的魚慢慢靠過來。停超過 26 秒牠們會看膩散開 |
| **魚** | 開啟面板：七個物種各一列，`+` 加一隻、`−` 撈走一隻。點物種名稱展開，可以指定花色加或撈。魚上限 20 隻、底棲 8 隻 |
| **聲音** | 水聲與音樂兩軌各自開關與音量。水聲預設開啟 30% |

### 會記得的事

關掉重開，缸子還是你離開時的樣子——**生物名冊（含花色）、燈光模式、聲音開關與音量、鏡頭角度**都會存進 `localStorage`。魚面板最底下有「重置成預設」，要按兩次才會執行。

如果你把缸子撈空了，重整之後它就是空的，不會偷偷幫你塞回預設的那批——空缸是一個合法的狀態，不是「沒有存檔」。

### 會自己發生的事

- **自動日夜**：開缸時讀系統時間，晚上七點到早上七點自動進夜燈模式，之後每分鐘對一次。手動按過「夜燈」就交還控制權
- **水流**：出水口在右後方，缸內形成一圈環流。魚會斜斜地頂著流游，飼料、懸浮微粒、氣泡全部跟著漂
- **餵太多會變濁**：沉底的飼料累積到一定量，水就開始混濁，焦散光斑減弱。等底棲清完才慢慢恢復
- **海馬勾水草**：海馬會挑一根草莖用尾巴勾住，停留 18–48 秒，隨水流輕輕搖擺，被嚇到才鬆開

---

## 技術總結

整份是一個 1.57 MB 的 HTML 檔，其中 1.08 MB 是內嵌音訊、0.38 MB 是內嵌模型，實際邏輯約 **106 KB / 2,993 行**。執行期只從 cdnjs 取 three.js r128，其餘零外部請求。

### 一、程序化動作：所有生物都沒有骨架

三個來源的模型**全部沒有骨架也沒有動畫**。所以每一種動作都是在 `MeshStandardMaterial` 的 `onBeforeCompile` 裡改頂點著色器做出來的，共用同一支 shader、靠 uniform 分流。目前有 **38 種自訂 uniform**。

**擺尾**：以頭（模型 +Z）為 0、尾為 1 取權重，做加權正弦位移。權重用 `t²`，所以身體前段幾乎不動、尾鰭擺幅最大。頻率與幅度即時綁在當下速度上——魚衝去搶食時尾巴明顯變快，發呆時幾乎不動。

```glsl
float _t = clamp(0.5 - transformed.z, 0.0, 1.0);   // 0 = 頭, 1 = 尾
float _w = _t * _t;
transformed.x += sin(uTime*uFreq + uPhase - _t*3.4) * uAmp * _w;
transformed.x += uTurn * _w * 0.30;                 // 轉彎時尾部額外偏擺
```

**河豚膨脹**：一開始沿法線外推，結果炸成一堆碎片——低面數球體是平滑分割過的，每個面各有一份頂點和自己的面法線，往法線推會讓相鄰面各走各的方向。改成沿「離模型中心的方向」外推，同位置的重複頂點得到完全相同的位移，接縫就閉合了。

```glsl
float _r = length(position);
if(_r > 0.0001) transformed += (position / _r) * uInflate * 0.24;
```

**步足與螯**：以「離身體中線的水平距離」`abs(position.x)` 當權重，軀幹完全不動、越靠外擺動越大；左右兩側相位差 180°，再依身體長軸給遞增相位，就成了依序推進的爬行步態。螯是另一組權重（身體前端 × 外側），做開合、外張／收攏、舉高三種姿態。

**站姿**：把步足權重**反相**，離中線越近抬得越高、腳尖權重為 1 完全不動——等於把腿撐直，肚子懸空只剩腳尖著砂。走得越快站得越高。

**海星**：中央隆起把薄片變成圓丘；五條腕足依方位角 `atan(z, x) * 5` 依序起伏。這裡有個坑——`atan(0,0)` 在 GLSL 是未定義的，中心頂點會回傳 NaN，就算乘上 0 權重 NaN 仍會擴散，整顆頂點會被丟到無限遠。加了 epsilon 避開。

**海馬**：背鰭顫動是把擺動頻率開到 13、幅度縮到 0.011——高頻小幅，就是海馬的特徵動作。牠**不套用「模型 +Z 對齊速度」的通則**，不然一往前游整隻就趴下去；改成永遠直立、只用偏航轉向，依速度前傾。旋轉順序設 `YXZ`，先偏航再前傾，身體才不會歪掉。

### 二、上色：一套雙色系統餵所有生物

每隻生物各自持有材質實例，所以能有獨立配色。做法是先把貼圖壓成明度（保留眼睛、鰭、殼的細節與陰影），再依模型局部座標分成背側／腹側兩色。分界線用兩層正弦弄成不規則波浪邊，每隻再隨機偏移，不會有兩隻長得一樣。

分界的柔和度是可調的：金魚的更紗花紋要硬邊（0.045），海馬的腹背過渡要柔（0.30）。另外還有體環斑紋，沿長軸打上細環，打散單一平塗色塊。

目前配色：金魚 11 種（含 4 種螢光）、神仙魚 6 種、海馬 5 種、河豚 5 種、燈魚 5 種、螯蝦 4 種、螃蟹 4 種、海星 4 種，合計 8 個物種 44 種花色。

**踩過的坑：ACES 的去飽和**。底棲生物一開始灰撲撲，我把明度乘數開到 2.3 想救它，結果更白。原因是白天的顏色要乘上一堆偏白的光再送進 ACES 色調映射，早就衝破 1.0 落在曲線的高亮肩部，而那段曲線的作用就是把顏色往白色收。關燈時反而鮮豔，因為夜間的自發光是直接把固有色加上去、不經過燈光。

最後的解法是反過來做：**明度砍掉四成**，改用三件事補回來——一個彩度補償 uniform（上完色後把 RGB 往離明度更遠的方向推，直接抵銷 ACES 的去飽和）、乘自己固有色的自發光補償（只加彩度不加白）、以及把粗糙度拉到 0.88／金屬度歸零殺掉白色高光。

### 三、水的表現

**焦散**是注入 `MeshStandardMaterial` 的，所以底砂、石頭、沉木、水草都吃得到光斑，而且保有真實陰影。強度隨離底高度衰減，也隨夜間與濁度減弱。

> 這裡也踩過坑：焦散原本用青綠色 `(0.42, 0.78, 0.68)` 加上去、強度又給太高，等於在整缸疊了一層綠光，石頭變薄荷綠、砂變白綠色。改成中性偏暖白並把強度砍到三分之一才正常。

水流的強度會依高度衰減——**水草叢會把水流打散，越靠底層越弱**。這不只是為了寫實：海馬的巡游速度只有 0.061 u/s，而水流是 0.24，若照單全收牠會被沖著跑，慢下來的設定完全看不出來，也永遠游不到水草上。另外每個物種有自己的受流程度，泳力強的被帶著走，泳力弱的幾乎不受影響。

**水流**是一個閉合環流：沿水面往左 → 順左壁下沉 → 沿底砂往右 → 順右壁上升。用流函數 `ψ = sin(πx)·sin(πy)` 推導速度場：

```
vx =  ∂ψ/∂y  ∝  sin(πx)·cos(πy)
vy = -∂ψ/∂x  ∝ -cos(πx)·sin(πy)
```

這樣**先天無散度**，魚和飼料不會被水流擠到某個角落堆積。出水口附近另外疊一道高斯衰減的射流。魚同時受到兩件事影響：被水帶著位移，以及主動往上游方向出力——結果就是斜斜地頂著流游。

**濁度**由沉底飼料數量驅動，控制霧的濃度（0.038 → 0.115）、霧色偏黃綠、焦散減弱七成、主燈調暗。變濁比變清快三倍。

其他：水面是雙層正弦的條紋波光（第一版用兩個正弦相乘，從低角度看會變成一排整齊的圓點）；懸浮微粒、氣泡柱、上方光柱（會跟著鏡頭方位轉，維持體積感）；玻璃是菲涅爾著色器，加 12 條矽膠邊框。

### 四、行為

`fishes` 陣列走同一套 boid 式操控（漫遊、分離、避障、缸壁、逃逸、追食、水流、手指好奇），各物種靠參數與少量分支分流。底棲的螯蝦、螃蟹、海星各有自己的更新函式。

**穿模**修過兩輪。第一版只有一個很弱的閃避力，而且碰撞半徑用名目值——但岩石是用噪聲把球面推出去的，實際半徑最多膨脹到名目的 1.4 倍。現在碰撞半徑改用真實包圍球，沉木沿每根樹枝登記碰撞球，更重要的是加了**位置層級的硬性排除**：積分完位置後如果還在碰撞球內就直接推出來，並把朝內的速度分量歸零。這是幾何保證，不是靠力量拉扯。底棲用 2D 版本——用球體在牠們所在高度的截面半徑來擋，所以可以貼著岩石底部走但不會走進去。

岩石本身也修過：原本用「砂面高度 + 半徑 × 0.42」估算擺放位置，噪聲膨脹後完全跟不上，石頭直接穿出缸底。現在建好幾何後算包圍盒，把最低點對齊砂面再往下埋兩成，並且**只繞 Y 軸旋轉**——只有這樣才能保證底部永遠朝下。

**海星曾經像浮在水中**：自轉軸用了 `rotation.z`，但 Three.js 的 Euler 預設是 XYZ 順序、先轉 Z 再轉 X，而局部 Z 是海星平面上的一條軸，繞它轉等於把海星翻出平面——牠不是貼著玻璃自轉，是在原地慢慢翻筋斗。自轉軸改到 `rotation.y`（海星自己的法線）就正常了。

### 五、水草

975 片葉子，合併成 19 個 `BufferGeometry`。每片葉子是自己生成的錐形 ribbon，帶 `aT`（高度比例）、`aPhase`、`aStiff` 三個自訂屬性，搖曳同樣是注入 `MeshStandardMaterial` 的頂點著色器。分後景水蘭、中景叢生、前景莫絲、紅蝴蝶點綴、沉木上的莫絲五種，各有自己的高度、寬度、錐度、傾倒與硬度分布。

### 六、加魚與撈魚

魚的增減放在一個面板裡，七個物種各一列顯示數量與 `+` `−`；點物種名稱可以展開該物種的所有花色，每個花色也有自己的數量與 `+` `−`，所以要加或撈哪一隻特定花色都指定得到。共 7 個物種、38 種花色。這個設計是刻意避開兩件事：**模式切換**（「點擊變成魚網再去點魚」在手機上沒有游標可變，而且會跟既有的「點缸壁敲玻璃」直接衝突，模式切換也最容易誤觸）和**破壞性操作**（「長按重置」只是把誤按的恐懼從一顆按鈕換到另一顆）。面板的好處是選種類、加、撈都在同一處，手機桌機行為一致，而且隨時看得到缸裡有幾隻什麼。

撈魚不是把資料列刪掉：按 `−` 會挑離鏡頭最近的那一隻（通常就是你正在看的那隻），讓牠掙扎著被往上帶離水面、破水時打出漣漪，附近的魚也跟著嚇一跳。

實作上，被撈的個體會**立刻**從自己的陣列裡移除，動畫交給一個獨立的 `scoops` 清單接手——這樣就不必在主迴圈裡邊走訪邊 splice，游的、爬的、貼玻璃的四種更新函式也都不用各自處理撈網狀態。幾何是同種共用的不能丟，但每隻各自持有一份材質（配色才能獨立），撈走時要 `dispose()`。

**沒有做死魚，是刻意的。** 一旦魚會死，這個缸子就從「可以放著不管的東西」變成一件待辦事項——會有罪惡感、會覺得該去餵、會不敢太久不開。那正好是這個專案想避開的東西。

### 七、記憶

存進 `localStorage` 的是一份名冊而不是完整快照：每個物種各花色的數量、燈光模式、聲音狀態、鏡頭角度。魚游到哪裡不存——重整後大家重新散開反而比較自然，存了也只是徒增體積與版本相容問題。

寫入是 debounce 400 ms 的，另外在 `pagehide` 與 `visibilitychange` 立刻落檔，避免關頁面時最後一筆改動掉了。名冊帶版本號，讀到舊版會先跑一次修補再升級。

另外有個跟記憶無關但同樣是「狀態沒收乾淨」的坑：切換 App 或鎖螢幕時，`pointerup` 與 `pointercancel` **都不會送達**，`pointers` 裡會留下一根永遠放不開的手指。回來之後下一根手指湊成兩點，拖曳就變成雙指縮放。要在 `visibilitychange`、`blur`、`pagehide`、`lostpointercapture` 四個地方把指標狀態歸零，並且對太久沒更新的指標做逾時清除。

**沙箱化的 iframe 光是讀取 `localStorage` 就會丟例外**（不是回傳 null），所以不能用 `typeof` 判斷，整段存取都得包在 `try` 裡。失敗時安靜地退回「每次重整都是預設」，不影響其他功能——這也表示在某些線上預覽環境裡記憶會失效，自己架起來就正常。

### 八、除錯出口

內部狀態全關在 IIFE 裡，從外面看不到，行為對不對只能用眼睛猜。`window.tankDebug()` 回傳一份唯讀快照（各物種隻數、幾隻海馬正勾著水草、著生點剩幾個、濁度、鏡頭參數），自動測試靠它才驗得到「海馬真的勾上水草了」這種事。

快照也用來守住幾何上的前提：著生點是照水草位置登記的，而石頭與沉木就在同一帶，沒檢查的話海馬會勾在石頭中間——測試現在會驗「所有著生點與硬景的最小淨空 > 0」以及「沒有任何海馬勾在硬景裡」。另外勾附那段會提前 `return`，跳過主迴圈的硬性排除，所以它得自己再擋一次。

這個出口不是可有可無的。海馬的勾附有個 bug 藏了很久：釋放條件寫成 `hitchT <= 0 || startle >= 0.05`，但 `hitchT` 在「已經選好著生點、還在游過去」的期間也是 0，於是海馬選定的下一幀就立刻放棄，永遠到不了草上。從畫面上只看得出「牠好像從來不勾草」，看不出是哪一步壞掉；有了快照才發現 `restT` 一直被重設成只會出現在釋放路徑的數值，一秒定位。

### 九、錯誤不要被吃掉

`Uncaught Error: Script error.` 是瀏覽器對**跨來源腳本**丟出的例外的統一遮蔽，訊息、行號、堆疊全部拿不到。這裡唯一的跨來源腳本是 cdnjs 的 three.js，所以任何從 three.js 內部丟出的例外都會變成這一行——**連頁內自己接的 `window.onerror` 也一樣看不到**。解法是給 `<script>` 加上 `crossorigin="anonymous"`（cdnjs 有回 `Access-Control-Allow-Origin: *`），細節就傳得回來了。

頁內接一次 `error` 與 `unhandledrejection`，把訊息、來源、行號、前四行堆疊寫進 console 與 `tankDebug().lastError`，同一個錯誤只在畫面上顯示前三次以免洗版。

主迴圈的 `requestAnimationFrame` 排在最前面，所以就算後面拋例外，迴圈本身不會斷——但 `renderer.render()` 到不了，症狀是**畫面凍住而聲音照跑**。單一錯誤不該讓整缸停擺，所以繪製主體包在 try 裡，接住、回報、繼續。自動測試會設 `window.tankStrict = true` 讓例外照常往外丟，否則測試就會變成睜眼瞎。

### 十、資產處理

**模型**共 12,324 頂點、6,392 面，壓成 **386 KB**：法線量化成 int8、頂點色量化成 uint8，交給 GPU 用 `normalized` 旗標還原；海星的三張 2048² 貼圖縮到 128² 並減色到 64 色。三隻海星共用同一份幾何與 UV，只換貼圖。

| | 頂點 | 面 | 備註 |
|---|---:|---:|---|
| 金魚 | 920 | 508 | 32² 貼圖 |
| 河豚 | 1,664 | 926 | 4 個材質群組，身體可換色 |
| 螯蝦 | 486 | 848 | 原始檔缺法線，重新計算 |
| 神仙魚 | 2,898 | 966 | 身體／鰭兩個群組，自帶頂點色 |
| 螃蟹 | 1,228 | 624 | 32² 貼圖 |
| 海星 | 162 | 260 | 一份幾何 × 3 張貼圖 |
| 海馬 | 538 | 784 | 3 個群組（身體／眼／瞳） |
| 大理石神仙魚 | 2,898 | 966 | 與神仙魚同一份幾何、不同的頂點色與拆法，3 個群組（身體／鰭／絲狀腹鰭） |
| 霓虹燈魚 | 1,530 | 510 | 身體／鰭兩個群組，自帶頂點色 |

模型方向不統一，抽取時就地轉正：螯蝦與螃蟹原本頭朝 -Z、神仙魚頭朝 -Z，都轉 180°；海馬本來就直立朝 +Z 不用轉，但改用**身高**當基準長度而不是深度。

後來加入的大理石神仙魚與霓虹燈魚**頭朝 +Z，不能再翻**——同一支產生腳本前後兩版的輸出朝向相反，是靠 `tools/aquarium_asset.py` 的交叉驗證抓到的。素材端的規格見 `docs/ASSET_CONTRACT.md`。

**音訊**兩軌都做過無縫循環：直接 loop 會在接點露出破綻，所以把尾巴 2.5 秒和開頭 2.5 秒交叉淡化後接回主體，循環點落在淡化區中間。原檔 256 kbps 立體聲共 2.8 MB，水聲重壓成 80 kbps 單聲道（擴散音源，單聲道聽不太出差別，解碼後還省一半記憶體），音樂保留立體聲壓到 96 kbps，合計 831 KB。

音訊圖是**整段建完才掛上去**的。原本是一邊建一邊指派，中途失敗就會留下一個設好的 `SND.ctx` 但沒有殘響節點；下一次呼叫走 `if(SND.ctx) return true` 的捷徑回報成功，接著任何音效一觸發就在 `connect(null)` 炸掉——症狀是敲玻璃偶爾報錯、重開又好了。現在失敗會把整組狀態清乾淨，所有音訊入口也都先問過 `audioReady()`，並各自包在 try 裡：音效壞掉不該讓你連缸都敲不了。

播放走 Web Audio 而非 `<audio>`，循環是取樣精確的，音量拉桿即時平滑過渡不會爆音。敲缸和餵食音效是即時合成的，走一個用衰減雜訊即時生成的脈衝響應做殘響。瀏覽器不允許未經互動就出聲，所以預設開啟的水聲會延到第一次碰畫面才真正播放。

---

## 測試

`tools/smoke-test.js` 是無頭冒煙測試。裝真正的 three.js r128，把 WebGL 那層換成假的 renderer，其餘場景圖程式碼照跑，再配一組假 DOM。

```bash
cd tools
npm install
node smoke-test.js ../水草缸.html
```

它會完整跑完初始化 + 15,000 幀以上的主迴圈，中途觸發餵食、日夜切換、聲音面板、指標互動，驗證面板結構（8 物種 / 44 花色）、逐一展開收合、用花色列指定加到撞上限、指定撈走、全部撈空再放回，連按十四次餵食把濁度推到滿。

**行為也被量成數字驗證**，不是只看有沒有例外：海馬勾附（80 段觀察裡有幾段勾著、著生點與硬景的最小淨空）、燈魚群游（同種魚的平均間距與前進方向一致度）、鰭根與身體是否同步（把著色器那段位移用 JS 重算一次，比較身體材質與鰭材質在交界處算出的差）。

記憶的部分是**真的跑一次重整**：用假的 `localStorage` 存檔，清掉 DOM 之後把整份主程式重新 eval 一遍，再比對還原後的名冊與音量是否一致，並確認空缸重整後仍然是空缸。

然後做一道 **GLSL 對帳**：攔截所有材質，把 `onBeforeCompile` 對真正的 `ShaderLib` 跑一次、遞迴展開所有 `#include`，雙向核對每個傳進去的 uniform 都必須在 GLSL 裡有宣告**而且**在宣告行之外真的被用到，反過來 GLSL 宣告的每個 uniform 也必須有對應的值。

這道檢查是有來由的。開發途中出過一次整頁掛掉：五個 uniform 的 JS 驅動端寫好了、著色器和宣告卻不存在，`spawnStar()` 讀 `uni.uDome.value` 時直接 TypeError，初始化中斷，載入遮罩永遠停在「注水中」。那次的成因是修改腳本「全部改完最後才寫檔」，中途一處字串比對失敗拋例外，導致在它之前**已經成功的修改全部沒進檔案**。現在腳本改成每改一處立刻落檔，而這道對帳專門擋「宣告有、實作沒有」這種靜默失敗。

> 註：假 renderer 不會真的編譯著色器，所以 GLSL 的語法錯誤仍需在瀏覽器確認一次。

---

## 檔案

```
水草缸.html          單一檔案，打開即用
README.md            這一份：這一缸的技術總結
LICENSE
docs/
  AQUARIUM_DESIGN_GUIDELINES.md   跨缸的設計與工程原則（Keep / Learned / Free）
  ASSET_CONTRACT.md               生物素材的規格，機器可檢查
tools/
  smoke-test.js      無頭冒煙測試 + GLSL 對帳
  aquarium_asset.py  素材驗證與中繼資料標記
  package.json
```

**要做下一個缸的話，先讀 `docs/AQUARIUM_DESIGN_GUIDELINES.md`，不要從這份 README 開始。** 這份是「這一缸怎麼做的」，那份才是「哪些值得繼承、哪些該你自己決定」。

## 相依

執行期只有 [three.js](https://threejs.org/) r128，從 cdnjs 載入。模型與音訊全部內嵌成 base64，離線也能跑。

---

## Credits

模型全部來自 [Poly Pizza](https://poly.pizza/)，授權 [CC BY 3.0](https://creativecommons.org/licenses/by/3.0/)：

- [Goldfish](https://poly.pizza/m/3GPUntjwqCa) — Poly by Google
- [Blowfish](https://poly.pizza/m/8DXeKkgTS_s) — jeremy
- [Seahorse](https://poly.pizza/m/d_36p3CahYa) — Poly by Google
- [Crayfish](https://poly.pizza/m/3Y2cocX0ILR) — Poly by Google
- [Crab](https://poly.pizza/m/2DgM36qZW2u) — Poly by Google
- [Starfish](https://poly.pizza/m/6H-0K9IEr56) — Poly by Google

神仙魚（含大理石花色）與霓虹燈魚為自製，由程序化腳本產生：低面數、頂點色、無骨架無貼圖，直接照 `docs/ASSET_CONTRACT.md` 的規格輸出。花色是「兩個顏色 + 一個判斷函式」的組合——函式收到面的座標與部位標籤，回傳 True 就上深色，所以要加新花紋只要多寫一個函式。

音訊來自 [Pixabay](https://pixabay.com/)：

- 音樂：*Soft Harmonic Breath (Short)* — [Gigi De La Ro](https://pixabay.com/users/gigidelaromusic-39322631/)
- 水聲：*Water Noises* — [Soul Serenity Sounds](https://pixabay.com/users/soul_serenity_sounds-43756209/)（ID 241049）

模型與音訊皆經過再處理（量化、貼圖縮減、重新著色、重新編碼、循環接合），詳見上方技術總結。

---

## 授權

**程式碼採 [MIT](LICENSE)。**

內嵌資產不適用 MIT，各自沿用上方 Credits 所列的授權：模型為 CC BY 3.0、音訊為 Pixabay Content License。

**CC BY 3.0 要求標示原作者**，所以出處同時寫在這份 README 和畫面左上角。要移除任一處之前，請先確認另一處仍在，否則會不符合授權要求。
